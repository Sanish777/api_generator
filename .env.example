# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=my_database
DB_PORT=3306

# Database Type (mysql or mssql)
DB_TYPE=mysql

# AI Provider Configuration
# Options: openai (default, same AI that Cursor IDE uses)
AI_PROVIDER=openai

# OpenAI API Configuration (same provider Cursor uses)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
